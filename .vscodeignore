.git
.gitignore
*.md
test/**
*.vsix

# Repo-local tooling and runtime artifacts — not part of the extension.
# (The slash-command definition and local Claude settings were shipping to
# every install; .vscode-bridge-port is written at runtime and a stale one
# has no business inside the package.)
.claude/**
.vscode-bridge-port
